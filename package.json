{
  "name": "claude-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "TZ=America/New_York bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile src/cli.ts --outfile dist/claude-dash",
    "dash": "bun run src/cli.ts",
    "snapshot": "bun run src/cli.ts --json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
